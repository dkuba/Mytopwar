import {cp,mkdir,rm}from'node:fs/promises';const root=new URL('../',import.meta.url),out=new URL('dist/',root);await rm(out,{recursive:true,force:true});await mkdir(out,{recursive:true});for(const p of ['index.html','styles.css','src','.nojekyll'])await cp(new URL(p,root),new URL(p,out),{recursive:true});console.log('Static browser assets ready in dist/.');
