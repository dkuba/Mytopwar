# Восстановление и контрольные точки

При возобновлении main указывал на f07a7621, а не на ранее заявленную полную версию.
Завершённые объекты Git были сохранены, но не все были привязаны к ветке.
Использована одна восстановленная реализация src/sim + src/render; незавершённый
альтернативный каталог src/v2 не добавляется в продукт.

Последовательно подтверждённые записи в main, без force push:

| Коммит | Фокус |
|---|---|
| f07a7621 | Контракты армии, мировых координат и сохранения |
| d32ae247 | Добор на трассе, пространственные ловушки, кампания и боссы |
| ed07293e | Оригинальная объёмная сцена и программный fallback |
| e1a4e2ec | Включение нового интерфейса и полного проверенного дерева |
| d272ec46 | Короткие диапазоны тестирования и сохранение после каждой миссии |

Следующий документационный коммит содержит свежие результаты и этот журнал.
Его идентификатор нужно брать из GitHub, а не вычислять или угадывать по переписке.

При следующем продолжении сначала прочитать main, затем npm run check.
Длинные campaign-прогоны делить через LEVEL_FROM/LEVEL_TO, отдельно для каждого SEED.
Границы аппаратной и реальной мобильной проверки остаются в отчёте.
